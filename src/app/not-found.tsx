import Error404Page from './404ErrorPage/page';

export default function NotFoundPage() {
  return <Error404Page />;
}
// This file is used to handle 404 errors in Next.js. When a user tries to access a page that does not exist, this component will be rendered, displaying a custom 404 error message.
